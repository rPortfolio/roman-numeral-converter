import java.util.Scanner;

class Main {
  public static void main(String[] args) {
		Gui gui = new Gui();
		// Scanner scanner = new Scanner(System.in);
		// boolean running = true;
		// while (running) {
		// 	System.out.println("Select Option:\n0. Convert to Roman Numerals\n1. Convert to Arabic Numerals\n2. Quit");
		// 	int option = scanner.nextInt();
		// 	switch (option) {
		// 		case 0:
		// 			System.out.println("Enter Integer from 1-3999");
		// 			int number = scanner.nextInt();
		// 			System.out.println("Roman Numeral: " + ArabToRoman.convert(number));
		// 			break;
		// 		case 1:
		// 			System.out.println("Enter Roman Numeral in Capital Letters");
		// 			scanner.nextLine();
		// 			String numeral = scanner.nextLine().strip();
		// 			int arab = RomanToArab.convert(numeral);
		// 			if (arab != -1) {
		// 				System.out.println("Arabic Numeral: " + arab);
		// 			} else {
		// 				System.out.println("Not a Valid Roman Numeral");
		// 			}
		// 			break;
		// 		default:
		// 			running = false;
		// 	}
		// 	System.out.println();
		// }
  }
}